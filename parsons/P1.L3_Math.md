---
layout: default
title: Parsons Problems [P1.L3 Math]
---

# P1.L3 Math

[Main Page](/Parson-Problems/index.html)

## Table of Contents
- [Math - Problem 1](#math---problem-1)
- [Math - Problem 2](#math---problem-2)
- [Math - Problem 3](#math---problem-3)
- [Math - Problem 4](#math---problem-4)
- [Math - Problem 5](#math---problem-5)
- [Math - Problem 6](#math---problem-6)
- [Math - Problem 7](#math---problem-7)
- [Math - Problem 8](#math---problem-8)

## Math - Problem 1

Goal: Ask the user for two numbers, then display both numbers and their sum.

<div id="Math_Problem1-sortableTrash" class="sortable-code"></div>
<div id="Math_Problem1-sortable" class="sortable-code"></div>
<div style="clear:both;"></div>
<p>
    <input id="Math_Problem1-feedbackLink" value="Get Feedback" type="button" />
    <input id="Math_Problem1-newInstanceLink" value="Reset Problem" type="button" />
</p>
<script type="text/javascript">
(function(){
  var initial = "first_num = int(input('What is your first number? '))\n" +
    "second_num = int(input('What is your second number? '))\n" +
    "sum_total = first_num + second_num\n" +
    "print('Your two numbers are ' + str(first_num) + ' and ' + str(second_num))\n" +
    "print('The sum is ' + str(sum_total))";
  var parsonsPuzzle = new ParsonsWidget({
    "sortableId": "Math_Problem1-sortable",
    "max_wrong_lines": 10,
    "grader": ParsonsWidget._graders.LineBasedGrader,
    "exec_limit": 2500,
    "can_indent": true,
    "x_indent": 50,
    "lang": "en",
    "show_feedback": true
  });
  parsonsPuzzle.init(initial);
  parsonsPuzzle.shuffleLines();
  $("#Math_Problem1-newInstanceLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.shuffleLines();
  });
  $("#Math_Problem1-feedbackLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.getFeedback();
  });
})();
</script>

## Math - Problem 2

Goal: Ask for first name, last name, and birth year, then print full name and age.

<div id="Math_Problem2-sortableTrash" class="sortable-code"></div>
<div id="Math_Problem2-sortable" class="sortable-code"></div>
<div style="clear:both;"></div>
<p>
    <input id="Math_Problem2-feedbackLink" value="Get Feedback" type="button" />
    <input id="Math_Problem2-newInstanceLink" value="Reset Problem" type="button" />
</p>
<script type="text/javascript">
(function(){
  var initial = "first_name = input('What is your first name? ')\n" +
    "last_name = input('What is your last name? ')\n" +
    "birth_year = int(input('What year were you born? '))\n" +
    "age = 2025 - birth_year\n" +
    "print('Hello ' + first_name + ' ' + last_name + ', you are ' + str(age) + ' years old.')";
  var parsonsPuzzle = new ParsonsWidget({
    "sortableId": "Math_Problem2-sortable",
    "max_wrong_lines": 10,
    "grader": ParsonsWidget._graders.LineBasedGrader,
    "exec_limit": 2500,
    "can_indent": true,
    "x_indent": 50,
    "lang": "en",
    "show_feedback": true
  });
  parsonsPuzzle.init(initial);
  parsonsPuzzle.shuffleLines();
  $("#Math_Problem2-newInstanceLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.shuffleLines();
  });
  $("#Math_Problem2-feedbackLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.getFeedback();
  });
})();
</script>

## Math - Problem 3

Goal: Ask for the radius of a circle, then display the circumference and area.

<div id="Math_Problem3-sortableTrash" class="sortable-code"></div>
<div id="Math_Problem3-sortable" class="sortable-code"></div>
<div style="clear:both;"></div>
<p>
    <input id="Math_Problem3-feedbackLink" value="Get Feedback" type="button" />
    <input id="Math_Problem3-newInstanceLink" value="Reset Problem" type="button" />
</p>
<script type="text/javascript">
(function(){
  var initial = "import math\n" +
    "pi_value = math.pi\n" +
    "radius = float(input('What is the radius? '))\n" +
    "area = pi_value * radius ** 2\n" +
    "circumference = 2 * pi_value * radius\n" +
    "print('The circumference is ' + str(circumference) + ' and the area is ' + str(area) + '.')";
  var parsonsPuzzle = new ParsonsWidget({
    "sortableId": "Math_Problem3-sortable",
    "max_wrong_lines": 10,
    "grader": ParsonsWidget._graders.LineBasedGrader,
    "exec_limit": 2500,
    "can_indent": true,
    "x_indent": 50,
    "lang": "en",
    "show_feedback": true
  });
  parsonsPuzzle.init(initial);
  parsonsPuzzle.shuffleLines();
  $("#Math_Problem3-newInstanceLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.shuffleLines();
  });
  $("#Math_Problem3-feedbackLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.getFeedback();
  });
})();
</script>

## Math - Problem 4

Goal: Calculate food order subtotal, tax, and total from fries and pop quantities.

<div id="Math_Problem4-sortableTrash" class="sortable-code"></div>
<div id="Math_Problem4-sortable" class="sortable-code"></div>
<div style="clear:both;"></div>
<p>
    <input id="Math_Problem4-feedbackLink" value="Get Feedback" type="button" />
    <input id="Math_Problem4-newInstanceLink" value="Reset Problem" type="button" />
</p>
<script type="text/javascript">
(function(){
  var initial = "num_fries = int(input('How many $4.00 fries do you want? '))\n" +
    "num_pops = int(input('How many $1.00 pops do you want? '))\n" +
    "print('Order: ' + str(num_fries) + ' fries and ' + str(num_pops) + ' pop.')\n" +
    "fries_total = num_fries * 4\n" +
    "pops_total = num_pops * 2\n" +
    "subtotal = fries_total + pops_total\n" +
    "print('Subtotal: $' + str(fries_total) + ' + $' + str(pops_total) + ' = $' + str(subtotal))\n" +
    "tax = subtotal * 0.15\n" +
    "total = subtotal + tax\n" +
    "print('Total: $' + str(subtotal) + ' + $' + '{:.2f}'.format(tax) + ' = $' + '{:.2f}'.format(total))";
  var parsonsPuzzle = new ParsonsWidget({
    "sortableId": "Math_Problem4-sortable",
    "max_wrong_lines": 10,
    "grader": ParsonsWidget._graders.LineBasedGrader,
    "exec_limit": 2500,
    "can_indent": true,
    "x_indent": 50,
    "lang": "en",
    "show_feedback": true
  });
  parsonsPuzzle.init(initial);
  parsonsPuzzle.shuffleLines();
  $("#Math_Problem4-newInstanceLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.shuffleLines();
  });
  $("#Math_Problem4-feedbackLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.getFeedback();
  });
})();
</script>

## Math - Problem 5

Goal: Calculate the average of three long-jump lengths.

<div id="Math_Problem5-sortableTrash" class="sortable-code"></div>
<div id="Math_Problem5-sortable" class="sortable-code"></div>
<div style="clear:both;"></div>
<p>
    <input id="Math_Problem5-feedbackLink" value="Get Feedback" type="button" />
    <input id="Math_Problem5-newInstanceLink" value="Reset Problem" type="button" />
</p>
<script type="text/javascript">
(function(){
  var initial = "jump_1 = float(input('Length of first jump: '))\n" +
    "jump_2 = float(input('Length of second jump: '))\n" +
    "jump_3 = float(input('Length of third jump: '))\n" +
    "average_jump = (jump_1 + jump_2 + jump_3) / 3\n" +
    "print('Average jump: ' + str(average_jump))";
  var parsonsPuzzle = new ParsonsWidget({
    "sortableId": "Math_Problem5-sortable",
    "max_wrong_lines": 10,
    "grader": ParsonsWidget._graders.LineBasedGrader,
    "exec_limit": 2500,
    "can_indent": true,
    "x_indent": 50,
    "lang": "en",
    "show_feedback": true
  });
  parsonsPuzzle.init(initial);
  parsonsPuzzle.shuffleLines();
  $("#Math_Problem5-newInstanceLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.shuffleLines();
  });
  $("#Math_Problem5-feedbackLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.getFeedback();
  });
})();
</script>

## Math - Problem 6

Goal: Convert a temperature from Celsius to Fahrenheit.

<div id="Math_Problem6-sortableTrash" class="sortable-code"></div>
<div id="Math_Problem6-sortable" class="sortable-code"></div>
<div style="clear:both;"></div>
<p>
    <input id="Math_Problem6-feedbackLink" value="Get Feedback" type="button" />
    <input id="Math_Problem6-newInstanceLink" value="Reset Problem" type="button" />
</p>
<script type="text/javascript">
(function(){
  var initial = "temp_c = float(input('Temperature (C): '))\n" +
    "temp_f = (temp_c * 9 / 5) + 32\n" +
    "print('Temperature in F: ' + str(temp_f) + 'F')";
  var parsonsPuzzle = new ParsonsWidget({
    "sortableId": "Math_Problem6-sortable",
    "max_wrong_lines": 10,
    "grader": ParsonsWidget._graders.LineBasedGrader,
    "exec_limit": 2500,
    "can_indent": true,
    "x_indent": 50,
    "lang": "en",
    "show_feedback": true
  });
  parsonsPuzzle.init(initial);
  parsonsPuzzle.shuffleLines();
  $("#Math_Problem6-newInstanceLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.shuffleLines();
  });
  $("#Math_Problem6-feedbackLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.getFeedback();
  });
})();
</script>

## Math - Problem 7

Goal: Use the Pythagorean theorem to calculate the hypotenuse from sides a and b.

<div id="Math_Problem7-sortableTrash" class="sortable-code"></div>
<div id="Math_Problem7-sortable" class="sortable-code"></div>
<div style="clear:both;"></div>
<p>
    <input id="Math_Problem7-feedbackLink" value="Get Feedback" type="button" />
    <input id="Math_Problem7-newInstanceLink" value="Reset Problem" type="button" />
</p>
<script type="text/javascript">
(function(){
  var initial = "import math\n" +
    "print('Pythagorean Theorem')\n" +
    "side_a = float(input('side a: '))\n" +
    "side_b = float(input('side b: '))\n" +
    "side_c_squared = side_a ** 2 + side_b ** 2\n" +
    "side_c = math.sqrt(side_c_squared)\n" +
    "print('Hypotenuse: ' + str(side_c))";
  var parsonsPuzzle = new ParsonsWidget({
    "sortableId": "Math_Problem7-sortable",
    "max_wrong_lines": 10,
    "grader": ParsonsWidget._graders.LineBasedGrader,
    "exec_limit": 2500,
    "can_indent": true,
    "x_indent": 50,
    "lang": "en",
    "show_feedback": true
  });
  parsonsPuzzle.init(initial);
  parsonsPuzzle.shuffleLines();
  $("#Math_Problem7-newInstanceLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.shuffleLines();
  });
  $("#Math_Problem7-feedbackLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.getFeedback();
  });
})();
</script>

## Math - Problem 8

Goal: Determine minimum toonies, loonies, quarters, dimes, and nickels for a change amount.

<div id="Math_Problem8-sortableTrash" class="sortable-code"></div>
<div id="Math_Problem8-sortable" class="sortable-code"></div>
<div style="clear:both;"></div>
<p>
    <input id="Math_Problem8-feedbackLink" value="Get Feedback" type="button" />
    <input id="Math_Problem8-newInstanceLink" value="Reset Problem" type="button" />
</p>
<script type="text/javascript">
(function(){
  var initial = "print('Change Calculator')\n" +
    "print('What amount of change is needed? ')\n" +
    "change = float(input())\n" +
    "print('You need:')\n" +
    "num_toonies = int(change // 2)\n" +
    "change = change % 2\n" +
    "print(str(num_toonies) + ' toonies')\n" +
    "num_loonies = int(change // 1)\n" +
    "change = change % 1\n" +
    "print(str(num_loonies) + ' loonies')\n" +
    "num_quarters = int(change // 0.25)\n" +
    "change = change % 0.25\n" +
    "print(str(num_quarters) + ' quarters')\n" +
    "num_dimes = int(change // 0.10)\n" +
    "change = change % 0.10\n" +
    "print(str(num_dimes) + ' dimes')\n" +
    "num_nickels = int(change // 0.05)\n" +
    "change = change % 0.05\n" +
    "if change > 0.02:\n" +
    "    num_nickels = num_nickels + 1\n" +
    "print(str(num_nickels) + ' nickels')";
  var parsonsPuzzle = new ParsonsWidget({
    "sortableId": "Math_Problem8-sortable",
    "max_wrong_lines": 10,
    "grader": ParsonsWidget._graders.LineBasedGrader,
    "exec_limit": 2500,
    "can_indent": true,
    "x_indent": 50,
    "lang": "en",
    "show_feedback": true
  });
  parsonsPuzzle.init(initial);
  parsonsPuzzle.shuffleLines();
  $("#Math_Problem8-newInstanceLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.shuffleLines();
  });
  $("#Math_Problem8-feedbackLink").click(function(event){
      event.preventDefault();
      parsonsPuzzle.getFeedback();
  });
})();
</script>
